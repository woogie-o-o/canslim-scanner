# canslim-quant-scanner runtime dependencies
# install: pip install -r requirements.txt

flask>=3.0
flask-compress>=1.14
flask-socketio>=5.3
simple-websocket>=1.0
gunicorn>=22.0
gevent>=24.0
gevent-websocket>=0.10

numpy>=1.24
pandas>=2.0
yfinance>=0.2.40
pyarrow>=14.0

requests>=2.31
urllib3>=1.26  # PoolManager 사용 — 1.26/2.x 모두 호환 (현재 환경 1.26.20 검증됨)
python-dotenv>=1.0
finnhub-python>=2.4

matplotlib>=3.7
Pillow>=10.0

plyer>=2.1
xlsxwriter>=3.1

# rebuild-trigger: 2026-05-23 — ensure flask-compress is installed on prod (silent gzip fail caused 11MB JSON timeouts)

# === 선제적 레짐 파악(Regime Detection) 모듈 — 2026-06-09 ===
# Linux 배포(x86_64)에는 휠 존재. 로컬 32-bit Python에는 휠이 없으므로
# regime_classifier.py는 import 실패 시 rule_based 폴백으로 무중단 동작한다.
scikit-learn>=1.3,<1.9
hmmlearn>=0.3.0
